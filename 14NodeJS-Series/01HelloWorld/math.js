function add(a, b) {
  return a + b;
}

function subract(a, b) {
  return a - b;
}

module.exports = {
    addFn: add,
    subFn: subract,
};

// exports.add = (a,b) => a + b;
// exports.sub = (a,b) => a - b;
